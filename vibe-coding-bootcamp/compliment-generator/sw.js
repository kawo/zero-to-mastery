// Service worker entry point. It stays at the site root so it can look after
// index.html (a service worker only controls pages in its own folder and
// below); the code itself is in js/sw.js.
importScripts('js/sw.js');
